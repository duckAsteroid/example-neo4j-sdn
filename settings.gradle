dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("libs.versions.toml"))
        }

        testLibs {
            from(files("testlibs.versions.toml"))
        }
    }
}
